/**
 * Database Configuration
 *
 * PostgreSQL was originally planned but the project uses MongoDB.
 * This file is kept as a placeholder. If PostgreSQL is needed later,
 * restore the pool setup from version control.
 */

// Not in use — MongoDB is the primary database.
// See ./mongodb.js for the active connection.

export default {};
